version: "3.9"

services:
  frontend:
    build:
      context: .
      dockerfile: docker/nginx/Dockerfile
    ports:
      - "8080:80"

  backend:
    build:
      context: .
      dockerfile: docker/node/Dockerfile
    volumes:
      - .:/app
